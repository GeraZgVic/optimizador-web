# ── Web server ────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9        # necesario para que FastAPI reciba UploadFile

# ── IA — Real-ESRGAN y sus dependencias ───────────────────────────────────────
# NOTA: instala PyTorch PRIMERO con el comando adecuado para tu sistema.
# Ver instrucciones en README.md (la versión CUDA cambia según tu GPU).
torch>=2.0.0
torchvision>=0.15.0

basicsr>=1.4.2                 # arquitecturas base (RRDBNet)
realesrgan>=0.3.0              # wrapper oficial de Real-ESRGAN
gfpgan>=1.3.8                  # restauración facial opcional

# ── Procesamiento de imágenes ──────────────────────────────────────────────────
Pillow>=10.0.0
numpy>=1.24.0
opencv-python-headless>=4.8.0  # cv2 sin GUI (más liviano que opencv-python)

# ── Utilidades ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0           # carga variables de entorno desde .env
celery>=5.6.0                  # cola de tareas asíncronas
redis>=7.0.0                   # cliente para Redis / backend de Celery
